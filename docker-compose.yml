services:
  frontend:
    build: 
      context: .
      target: dev
    
    ports:
      - "3000:3000"
    
    environment:
      - HOST=0.0.0.0
      - PORT=3000
      - CI=true
      - CHOKIDAR_USEPOLLING=true
    
    volumes:
      - .:/app
      - /app/node_modules